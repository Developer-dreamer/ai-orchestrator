package prompt

import (
	"context"
	"time"
)

func SendPromptUseCase(ctx context.Context, messageID, entity string) error {
	// TODO: replace this placeholder with actual business logic for processing the message entity.
	time.Sleep(time.Second)
	return nil
}
